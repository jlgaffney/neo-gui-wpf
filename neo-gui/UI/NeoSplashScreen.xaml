<Window x:Class="Neo.UI.NeoSplashScreen"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:properties="clr-namespace:Neo.Properties"
        Title="{x:Static properties:Strings.NeoGuiTitle}"
        Width="525" Height="340" ResizeMode="NoResize" WindowStyle="None">

 <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Image Grid.Row="0" HorizontalAlignment="Center" VerticalAlignment="Center" />

        <Label Grid.Row="1" Content="{x:Static properties:Strings.NeoGuiTitle}" HorizontalAlignment="Center" VerticalAlignment="Center" />

        <ProgressBar Grid.Row="2" IsIndeterminate="True" HorizontalAlignment="Center" VerticalAlignment="Center" />
    </Grid>
</Window>