<controls:NeoWindow x:Class="Neo.UI.Views.Voting.ElectionView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:Neo.UI.Controls"
        xmlns:votingVm="clr-namespace:Neo.UI.ViewModels.Voting"
        Title="Election" MinWidth="500" SizeToContent="WidthAndHeight" ResizeMode="NoResize">
    <controls:NeoWindow.DataContext>
        <votingVm:ElectionViewModel />
    </controls:NeoWindow.DataContext>
    
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            
            <Label Grid.Column="0" Content="Bookkeeper:" Margin="0 0 4 0" VerticalAlignment="Center" />
            <ComboBox Grid.Column="1" ItemsSource="{Binding BookKeepers}" SelectedItem="{Binding SelectedBookKeeper}" HorizontalAlignment="Stretch" VerticalAlignment="Center" />
        </Grid>

        <Button Grid.Row="1" Content="OK" Width="60" Margin="0 12 0 0" HorizontalAlignment="Right" Command="{Binding OkCommand}" IsEnabled="{Binding OkEnabled}" />
    </Grid>
</controls:NeoWindow>