<controls:NeoWindow x:Class="Neo.UI.Views.Accounts.ViewContractView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:Neo.UI.Controls"
        xmlns:accountsVm="clr-namespace:Neo.UI.ViewModels.Accounts"
        Title="View Contract" Height="460" Width="480" SizeToContent="Height" ResizeMode="CanResizeWithGrip">
    <controls:NeoWindow.DataContext>
        <accountsVm:ViewContractViewModel />
    </controls:NeoWindow.DataContext>
    
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        
        <Grid Grid.Row="0" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Label Grid.Row="0" Grid.Column="0" Content="Address:" HorizontalAlignment="Right" VerticalAlignment="Center" />
            <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding Address, Mode=OneWay}" Margin="4" Padding="2" HorizontalAlignment="Stretch" />

            <Label Grid.Row="1" Grid.Column="0" Content="Script Hash:" HorizontalAlignment="Right" VerticalAlignment="Center" />
            <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding ScriptHash, Mode=OneWay}" Margin="4" Padding="2" HorizontalAlignment="Stretch" />

            <Label Grid.Row="2" Grid.Column="0" Content="Parameter List:" HorizontalAlignment="Right" VerticalAlignment="Center" />
            <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding ParameterList, Mode=OneWay}" Margin="4" Padding="2" HorizontalAlignment="Stretch" />
        </Grid>
        
        <GroupBox Grid.Row="1" Header="Redeem Script" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <TextBox Text="{Binding RedeemScriptHex, Mode=OneWay}" IsReadOnly="True" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" TextWrapping="Wrap"
                     HorizontalScrollBarVisibility="Hidden"  VerticalScrollBarVisibility="Visible" />
        </GroupBox>

        <Button Grid.Row="2" Grid.ColumnSpan="2" Content="Close" Width="60" Margin="0 8 0 0" HorizontalAlignment="Right" Command="{Binding CloseCommand}" />
    </Grid>
</controls:NeoWindow>